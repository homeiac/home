diff --git a/snippet_uploader.py b/snippet_uploader.py
--- a/snippet_uploader.py
+++ b/snippet_uploader.py
@@ -1,5 +1,13 @@
-import os
+import os
+import logging
+import tempfile
+from pathlib import Path
 from typing import List, Sequence
 from ssh_utils import SSHClient, file_exists, upload_regular_file

+logger = logging.getLogger(__name__)
+
 def upload_snippets(
-    ssh, host_name, snippets, force=False
+    ssh: SSHClient,
+    host_name: str,
+    snippets: Sequence[str],
+    force: bool = False,
 ) -> None:
@@ -7,13 +15,30 @@ def upload_snippets(
     Render and upload snippet templates for a given Proxmox host.
     """
-    # 1. Render and upload the TEMPLATE_SNIPPET
-    rendered = render_template(host_name)
-
-    # Write rendered template to a temp file
-    p:
-        tmp.write(rendered)
-        tmp_path = tmp.name
-
-    remote_path = f"{REMOTE_SNIPPET_DIR}/{host_name}-{TEMPLATE_SNIPPET}"
-    if not force and file_exists(ssh, remote_path):
-        print(f"ℹ️  Skipping rendered snippet for {host_name}: already exists")
-    else:
-        print(f"⬆️  Uploading {host_name}-{TEMPLATE_SNIPPET} to primary")
-        upload_regular_file(ssh, tmp_path, remote_path)
-    os.unlink(tmp_path)
+    # 1. Render and upload the TEMPLATE_SNIPPET
+    rendered = render_template(host_name)
+
+    # Write rendered template to a temporary file
+    with tempfile.NamedTemporaryFile(delete=False) as tmp_file:
+        tmp_file.write(rendered.encode())
+        temp_path = tmp_file.name
+    try:
+        remote_path = f"{REMOTE_SNIPPET_DIR}/{host_name}-{TEMPLATE_SNIPPET}"
+        if not force and file_exists(ssh, remote_path):
+            logger.info("ℹ️  Skipping rendered snippet for %s: already exists", host_name)
+        else:
+            logger.info("⬆️  Uploading %s to %s", TEMPLATE_SNIPPET, remote_path)
+            upload_regular_file(ssh, temp_path, remote_path)
+    finally:
+        Path(temp_path).unlink(missing_ok=True)
+
+    # 2. Copy every other snippet exactly once
+    for snippet_name in snippets:
+        if snippet_name == TEMPLATE_SNIPPET:
+            continue  # skip the main template
+        remote_path = f"{REMOTE_SNIPPET_DIR}/{host_name}-{snippet_name}"
+        upload_regular_file(ssh, snippet_name, remote_path)

diff --git a/config.py b/config.py
--- a/config.py
+++ b/config.py
@@ -1,3 +1,6 @@
-import os
-from typing import List
+import os
+from typing import List

 class Config:
@@ -4,6 +7,13 @@ class Config:
     LEGACY_SNIPPET_URLS = [
         "https://releases.ubuntu.com/24.04.2/ubuntu-24.04.2-desktop-amd64.iso",
     ]
+
+    @classmethod
+    def get_pve_ips(cls) -> List[str]:
+        """Return Proxmox node IPs from the PVE_IPS env var (comma-separated)."""
+        raw = os.getenv("PVE_IPS", "")
+        return [ip.strip() for ip in raw.split(",") if ip.strip()]
+
     @staticmethod
     def get_nodes():
         ...
