# Frigate Coral LXC Deployment Configuration
# GitHub Issue: #168
# Target Host: still-fawn

# Proxmox Host
PVE_HOST="192.168.4.17"
PVE_HOST_NAME="still-fawn"

# Container ID - SET AFTER CREATION
VMID="110"

# Coral USB Device (from lsusb on still-fawn)
CORAL_BUS="004"
CORAL_DEV="002"
CORAL_VENDOR_UNINIT="1a6e"  # Global Unichip (uninitialized)
CORAL_VENDOR_INIT="18d1"   # Google (initialized)

# Script directory
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
