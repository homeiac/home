# voice-pe Scripts

> Auto-generated by `scripts/generate-readme.sh` - do not edit manually

## Scripts

| Script | Description |
|--------|-------------|
| `check-assist-services.sh` | Check available assist/conversation services |
| `check-device-logs.sh` | Check Voice PE ESPHome logs and HA connection status |
| `check-entities.sh` | Check Voice PE entities in HA |
| `check-esphome-addon.sh` | Check ESPHome addon status and Voice PE adoption |
| `check-esphome-device.sh` | Check ESPHome device info in HA device registry |
| `check-esphome-entries.sh` | Check ESPHome config entries in HA |
| `check-esphome-via-chief-horse.sh` | Check ESPHome addon status via chief-horse (direct access to HA) |
| `check-logs.sh` | Check ESPHome logs from Voice PE |
| `create-voice-approval-intents.sh` | Create HA intents for voice approval (yes/no) |
| `deploy-ha-proxy.sh` | Deploy ha-proxy.service to pve host<br>This socat proxy allows Voice PE to reach Home Assistant across network segments |
| `diagnose-connectivity.sh` | Voice PE Connectivity Diagnosis<br>Quickly identifies why Voice PE isn't connecting to Home Assistant<br>Usage: ./diagnose-connectivity.sh |
| `docker-compile.sh` | Compile ESPHome firmware using Docker (same image as HAOS)<br>Much faster than HAOS VM - uses all Mac cores |
| `erase-flash.sh` | Erase Voice PE flash completely |
| `fetch-esphome-yaml.sh` | Fetch Voice PE ESPHome YAML from HAOS |
| `find-esphome-config.sh` | Find where ESPHome stores its config on HAOS |
| `flash-file.sh` | Flash a specific firmware file to Voice PE |
| `haos-docker-exec.sh` | Run command in HAOS docker container<br>Usage: ./haos-docker-exec.sh <container> <command><br>Example: ./haos-docker-exec.sh addon_5c53de3b_esphome "ls /config" |
| `haos-esphome-cat.sh` | Read file from ESPHome addon in HAOS via docker exec<br>Usage: ./haos-esphome-cat.sh <path-in-esphome-container><br>Example: ./haos-esphome-cat.sh /config/esphome/home-assistant-voice-09f5a3.yaml |
| `haos-esphome-cp.sh` | Copy file to ESPHome addon in HAOS<br>Creates backup of existing file first, then copies local file over<br>Usage: ./haos-esphome-cp.sh <local-file> <remote-path-in-container><br>Example: ./haos-esphome-cp.sh ./configs/voice.yaml /config/esphome/voice.yaml |
| `haos-esphome-ls.sh` | List files in ESPHome addon in HAOS via docker exec<br>Usage: ./haos-esphome-ls.sh [path]<br>Example: ./haos-esphome-ls.sh /config/esphome |
| `haos-esphome-write.sh` | Write content to file in ESPHome addon via base64 encoding<br>Creates backup first<br>Usage: ./haos-esphome-write.sh <local-file> <remote-path> |
| `list-services.sh` | List all Voice PE services in HA |
| `local-compile-ota.sh` | Compile ESPHome firmware locally on Mac and OTA flash to Voice PE<br>Much faster than compiling on HAOS (2-core VM) |
| `ota-upload-long-timeout.sh` | OTA upload with very long timeout for slow WiFi (power-save issue) |
| `reload-esphome-entry.sh` | Reload ESPHome config entry to force HA reconnection after firmware update<br>This is REQUIRED after flashing new firmware with API actions |
| `serial-monitor-reset.sh` | Serial monitor with ESP32 reset trigger<br>Sends DTR/RTS toggle to trigger reset and capture boot logs<br>Usage: ./serial-monitor-reset.sh [device] [duration_seconds] |
| `serial-monitor-timeout.sh` | Serial monitor with hard timeout - won't hang<br>Uses timeout command to guarantee termination<br>Usage: ./serial-monitor-timeout.sh [device] [duration] [filter]<br>Example: ./serial-monitor-timeout.sh /dev/cu.usbmodem12201 30 "api|error" |
| `serial-monitor.sh` | Serial monitor for ESP32 using pyserial<br>Usage: ./serial-monitor.sh [device] [duration_seconds]<br>Unlike 'cat /dev/cu.usbmodem*' which hangs, this properly handles<br>the serial connection with timeout and clean exit. |
| `set-assistant.sh` | Set Voice PE assistant pipeline |
| `set-volume.sh` | Set Voice PE volume |
| `test-led-color.sh` | Test LED segment with specific color |
| `test-per-led.sh` | Test LED effects on Voice PE |
| `test-piper-synthesis-speed.sh` | Test Piper TTS synthesis speed independently of Voice PE<br>This isolates whether the bottleneck is Piper or the Voice PE streaming |
| `test-speak-response.sh` | Test the Claude Speak Response automation |
| `test-start-conversation.sh` | Test assist_satellite.start_conversation directly |
| `test-tts-direct.sh` | Test TTS directly to Voice PE media player |
| `test-tts-google.sh` | Test TTS to Google Home speaker |
| `test-tts-message.sh` | Simple TTS test to Voice PE with timing |
| `trace-tts-packets.sh` | Trace packets during Voice PE TTS to identify latency source<br>This script captures network traffic while triggering a TTS announcement |
| `usb-flash-esptool.sh` | Flash Voice PE via USB using esptool (bypasses Docker USB issues on Mac) |
| `usb-upload.sh` | USB upload to Voice PE (bypasses WiFi entirely) |

*Generated: 2026-01-18*
