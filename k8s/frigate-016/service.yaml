apiVersion: v1
kind: Service
metadata:
  name: frigate
  namespace: frigate
  labels:
    app: frigate
spec:
  type: LoadBalancer
  selector:
    app: frigate
  ports:
    - name: web
      port: 5000
      targetPort: 5000
      protocol: TCP
    - name: rtsp
      port: 8554
      targetPort: 8554
      protocol: TCP
    - name: webrtc-tcp
      port: 8555
      targetPort: 8555
      protocol: TCP
---
# Separate UDP service for WebRTC (MetalLB handles this)
apiVersion: v1
kind: Service
metadata:
  name: frigate-webrtc-udp
  namespace: frigate
  labels:
    app: frigate
spec:
  type: LoadBalancer
  selector:
    app: frigate
  ports:
    - name: webrtc-udp
      port: 8555
      targetPort: 8555
      protocol: UDP
