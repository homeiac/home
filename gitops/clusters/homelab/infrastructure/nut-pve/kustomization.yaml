apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  # Secrets (SOPS encrypted)
  - secrets/pve-ssh-key.sops.yaml
  - secrets/sms-gateway-creds.sops.yaml
  - secrets/ghcr-creds.sops.yaml
  # Deploy Job moved to ./deploy/ with separate Flux Kustomization (force:true)
  # See: infrastructure/nut-pve-deploy/flux-kustomization.yaml
  # Prometheus monitoring
  - service-nut-exporter.yaml
  - servicemonitor-nut-exporter.yaml
