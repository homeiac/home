apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  # Secrets (SOPS encrypted)
  - secrets/pve-ssh-key.sops.yaml
  - secrets/sms-gateway-creds.sops.yaml
  - secrets/ghcr-creds.sops.yaml
  # Deployment Job (triggered by image update)
  - job-deploy.yaml
  # Flux Image Automation (disabled - image-reflector-controller not installed)
  # - image-policy.yaml
  # Prometheus monitoring
  - service-nut-exporter.yaml
  - servicemonitor-nut-exporter.yaml
