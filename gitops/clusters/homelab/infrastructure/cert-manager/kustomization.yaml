apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
  - cloudflare-secret.yaml
# NOTE: CRD-dependent resources (ClusterIssuer, Certificate) are in
# infrastructure/cert-manager-config/ with dependsOn ordering
