apiVersion: v1
kind: Service
metadata:
  name: ollama
  namespace: ollama
spec:
  selector:
    app: ollama-gpu
  ports:
    - port: 80
      targetPort: 11434
      nodePort: 30080
      protocol: TCP

