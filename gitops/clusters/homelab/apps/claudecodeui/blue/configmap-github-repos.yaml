apiVersion: v1
kind: ConfigMap
metadata:
  name: github-repos
  namespace: claudecodeui
data:
  # One repo per line: <clone-url> <directory-name> [display-name]
  # Lines starting with # are ignored
  # Uses SSH URLs — requires github-ssh-key secret to be deployed
  repos: |
    git@github.com:homeiac/home.git home home
